# Transaction Graph - Dependencies
# Install (local dev): uv pip install -r requirements.txt
# Install (with Spark): uv pip install -r requirements.txt -r requirements-spark.txt
# Install (with GNN):   uv pip install -r requirements.txt -r requirements-gnn.txt

# Core
pandas>=2.0
numpy>=1.24
networkx>=3.0
pyarrow>=12.0

# Graph Analysis
python-igraph>=0.10
leidenalg>=0.9

# Visualization
pyvis>=0.3
matplotlib>=3.7

# ML — Look-Alike scoring
lightgbm>=4.0          # GBM: faster than sklearn, native categoricals
shap>=0.44             # Model interpretability: direction + magnitude per feature
umap-learn>=0.5        # Dimensionality reduction for cluster visualization
hdbscan>=0.8           # Density clustering: finds k automatically, marks outliers

# Development
pytest>=7.0
ruff>=0.1
